<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <title><%= book ? book.title : 'Book Not Found' %> | Book Haven</title>
    <style>
        .book-cover {
            max-width: 300px;
            height: auto;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .book-details {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 2rem;
        }
        .detail-label {
            font-weight: 600;
            color: #5a3d2b;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand" href="/">Book Haven</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
              <% if (user.role === "admin") { %>
                <li class="nav-item">
                  <a class="nav-link" href="/inventory"><i class="bi bi-box-seam"></i> Inventory</a>
                </li> 
                <li class="nav-item">
                  <a class="nav-link" href="/addBook"><i class="bi bi-plus-circle"></i> Add Book</a>
                </li> 
              <% } %>
              <li class="nav-item">
                <a class="nav-link" href="/cart"><i class="bi bi-cart"></i> Cart</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/logout"><i class="bi bi-box-arrow-right"></i> Logout</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container py-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="mb-0">Welcome, <%= user.username %> <span class="badge bg-secondary"><%= user.role %></span></h2>
            <% if (user.role === "admin") { %>
                <a href="/inventory" class="btn btn-outline-secondary"><i class="bi bi-arrow-left"></i> Back to Inventory</a>
            <% } else { %>
                <a href="/shopping" class="btn btn-outline-primary"><i class="bi bi-arrow-left"></i> Continue Shopping</a>
            <% } %>
        </div>

        <div class="text-center mb-4">
            <h1><%= book ? book.title : 'Book Not Found' %></h1>
        </div>

        <% if (book) { %>
        <div class="row book-details">
            <div class="col-md-4 text-center">
                <img src="/images/<%= book.cover %>" alt="<%= book.title %>" class="img-fluid book-cover mb-4">
                <% if (user.role === "user") { %>
                    <form action="/add-to-cart/<%= book.bookId %>" method="POST" class="mt-3">
                        <div class="input-group mb-3">
                            <input type="number" name="quantity" class="form-control" value="1" min="1" max="<%= book.quantity %>">
                            <button class="btn btn-primary" type="submit">Add to Cart</button>
                        </div>
                    </form>
                <% } %>
            </div>
            <div class="col-md-8">
                <p><span class="detail-label">Author:</span> <%= book.author %></p>
                <p><span class="detail-label">ISBN:</span> <%= book.isbn %></p>
                <p><span class="detail-label">Genre:</span> <span class="badge bg-info"><%= book.genre %></span></p>
                <p><span class="detail-label">Availability:</span> 
                    <% if (book.quantity > 0) { %>
                        <span class="badge bg-success"><%= book.quantity %> in stock</span>
                    <% } else { %>
                        <span class="badge bg-danger">Out of stock</span>
                    <% } %>
                </p>
                <p><span class="detail-label">Price:</span> <span class="h4 text-primary">$<%= book.price.toFixed(2) %></span></p>
                
                <% if (user.role === "admin") { %>
                    <div class="mt-4">
                        <a href="/updateBook/<%= book.bookId %>" class="btn btn-warning me-2"><i class="bi bi-pencil"></i> Edit Book</a>
                        <a href="/deleteBook/<%= book.bookId %>" class="btn btn-danger" 
                           onclick="return confirm('Are you sure you want to delete this book?')">
                           <i class="bi bi-trash"></i> Delete Book
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
        <% } else { %>
            <div class="alert alert-danger">
                <h4 class="alert-heading">Book Not Found</h4>
                <p>The requested book could not be found in our collection.</p>
            </div>
        <% } %>
    </div>
</body>
</html>