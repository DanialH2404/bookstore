<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Book Inventory</title>
  <style>
    .book-cover {
      max-height: 100px;
      width: auto;
    }
    .action-links a {
      margin: 0 5px;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Bookstore App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/addBook">Add Book</a>
          </li> 
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li> 
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h3>Welcome  <%= user.username %> <%= user.role %></h3>
      <a href="/shopping" class="btn btn-outline-secondary">View Customer Store</a>
    </div>
    
    <h2 class="text-center mb-4">Book Inventory Management</h2>
    
    <div class="table-responsive">
      <table class="table table-hover align-middle">
        <thead class="table-dark">
          <tr>
            <th>Cover</th>
            <th>Title</th>
            <th>Author</th>
            <th>ISBN</th>
            <th>Genre</th>
            <th>Stock</th>
            <th>Price</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% for(let i=0; i < books.length; i++) { %>
            <tr>
              <td>
                <img src="/images/<%= books[i].cover %>" alt="Book cover" class="book-cover img-thumbnail">
              </td>
              <td>
                <a href="/book/<%= books[i].bookId %>"><%= books[i].title %></a>
              </td>
              <td><%= books[i].author %></td>
              <td><%= books[i].isbn %></td>
              <td><%= books[i].genre %></td>
              <td><%= books[i].quantity %></td>
              <td>
  $<%= typeof books[i].price === 'number' ? books[i].price.toFixed(2) : parseFloat(books[i].price || 0).toFixed(2) %>
</td>
              <td class="action-links">
                <a href="/updateBook/<%= books[i].bookId %>" class="btn btn-sm btn-warning">Edit</a>
                <a href="/deleteBook/<%= books[i].bookId %>" 
                   class="btn btn-sm btn-danger" 
                   onclick="return confirm('Are you sure you want to delete this book?')">
                  Delete
                </a>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>